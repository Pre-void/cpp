#include <stdio.h>
#include <stdlib.h>
#define max 10001
int main(int argc, char const *argv[])
{
    char c[max];
    // c=getchar();
    scanf("%s",&c);
    printf("%s",c);
    system("pause");
    return 0;
}
